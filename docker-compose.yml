version: "3.9"
services:
  app:
    build: .
    container_name: rag-notebook-app
    command: pnpm start
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - QDRANT_URL=${QDRANT_URL}
      - QDRANT_API_KEY=${QDRANT_API_KEY}
